---
description: Deploy a coordinated agent fleet with interactive role, model, and effort selection
---

Load and follow the fleet-commander agent skill exactly. Walk the user through the 5-message interactive flow (Task → Roles → Counts → Model+Effort → Confirm), then dispatch agents in phased execution.

If the user provided arguments: $ARGUMENTS — use that as the task description and skip Message 1.
